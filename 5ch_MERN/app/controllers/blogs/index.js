const { getBlogsByGenre } = require('./getBlogsByGenre')
const { getNewBlogs } = require('./getNewBlogs')
const { getBlogsByBlogsite } = require('./getBlogsByBlogsite')

module.exports = {
  getBlogsByGenre,
  getNewBlogs,
  getBlogsByBlogsite
}
